const initialSTate = {
    productList: []
}

// action@ {}
// vori mej ka 2 parametr
//  type, payload

function ProductReducer(state = initialSTate, action) {

    switch (action.type) {

        default: {
            return state
        }

    }
}

export default ProductReducer